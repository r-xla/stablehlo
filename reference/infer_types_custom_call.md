# Infer types for custom call

Infer the output types for a custom call operation.

## Usage

``` r
infer_types_custom_call(...)
```

## Arguments

- ...:

  Input value types and custom call config.

## Value

(`ValueTypes`)  
The output types (empty for side-effect only calls).
