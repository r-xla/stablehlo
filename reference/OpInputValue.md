# OpInputValue

This represents a value that can be used as input to an operation.

## Usage

``` r
OpInputValue(id)
```

## Arguments

- id:

  ([`ValueId`](ValueId.md))  
  The id of the value.

## Value

(`OpInputValue`)
