# Convert environment to name

This function converts an environment to a name. Can only be called
within a `repr` call.

## Usage

``` r
repr_env2name(x)
```

## Arguments

- x:

  The environment to convert to a name.

## Value

`character(1)`
