# Represent an R value in stableHLO

Convert R value to StableHLO string representation

## Usage

``` r
r_to_stablehlo_string(value, dtype)
```

## Arguments

- value:

  (any)  
  The R value to convert

- dtype:

  (`character(1)`)  
  The element type to use.

## Value

`character(1)`
