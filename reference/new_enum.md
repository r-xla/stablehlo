# Create a new enum class

Create a new enum class

## Usage

``` r
new_enum(class_name, variants)
```

## Arguments

- class_name:

  Name of the enum class

- variants:

  Character vector of valid variants

## Value

Constructor function for the enum
