% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op-convolution.R
\name{infer_types_convolution}
\alias{infer_types_convolution}
\alias{hlo_convolution}
\title{Convolution Operator}
\usage{
infer_types_convolution(
  lhs,
  rhs,
  dimension_numbers,
  precision_config,
  window_strides,
  padding,
  lhs_dilation,
  rhs_dilation,
  window_reversal,
  feature_group_count,
  batch_group_count
)

hlo_convolution(
  lhs,
  rhs,
  dimension_numbers,
  window_strides,
  padding,
  lhs_dilation = NULL,
  rhs_dilation = NULL,
  window_reversal = NULL,
  feature_group_count = 1L,
  batch_group_count = 1L,
  precision_config = c("DEFAULT", "DEFAULT")
)
}
\arguments{
\item{lhs}{(\code{\link{FuncValue}})\cr
The input tensor (typically \verb{[batch, spatial..., feature]}).}

\item{rhs}{(\code{\link{FuncValue}})\cr
The kernel tensor.}

\item{dimension_numbers}{(\code{\link{ConvDimensionNumbers}})\cr
The convolution dimension numbers describing the layout of \code{lhs}, \code{rhs},
and the result.}

\item{precision_config}{(\code{character(2)})\cr
Two precision specifiers (one for each operand), each one of
\code{"DEFAULT"}, \code{"HIGH"} or \code{"HIGHEST"}. Defaults to \code{c("DEFAULT", "DEFAULT")}.}

\item{window_strides}{(\code{integer()})\cr
Stride of the kernel window in each spatial dimension.
Length \code{N - 2} where \code{N = rank(lhs)}.}

\item{padding}{(\code{matrix})\cr
\verb{[N - 2, 2]} integer matrix of \verb{(low, high)} padding for each spatial
dimension.}

\item{lhs_dilation}{(\code{integer()})\cr
Dilation factor applied to \code{lhs} (a.k.a. transpose-conv stride).
Length \code{N - 2}. Defaults to all 1's.}

\item{rhs_dilation}{(\code{integer()})\cr
Dilation factor applied to \code{rhs} (a.k.a. atrous-conv rate).
Length \code{N - 2}. Defaults to all 1's.}

\item{window_reversal}{(\code{logical()})\cr
Whether to reverse the kernel along each spatial dimension.
Length \code{N - 2}. Defaults to all \code{FALSE}.}

\item{feature_group_count}{(\code{integer(1)})\cr
Number of feature groups (for grouped / depthwise convolution).
Defaults to 1.}

\item{batch_group_count}{(\code{integer(1)})\cr
Number of batch groups. Defaults to 1.}
}
\value{
\code{\link{FuncValue}}
}
\description{
Computes dot products between windows of \code{lhs} and slices of \code{rhs}.
See \url{https://openxla.org/stablehlo/spec#convolution} for details.

All dimension indices are 0-based.
}
