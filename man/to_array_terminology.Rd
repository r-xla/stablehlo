% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{to_array_terminology}
\alias{to_array_terminology}
\title{Report arrays instead of tensors}
\usage{
to_array_terminology(x)
}
\arguments{
\item{x}{(\code{condition})\cr Condition whose message should be rewritten.
Other objects are returned unchanged.}
}
\value{
The condition, with array terminology applied to its message.
}
\description{
Rewrites a condition's message to use array terminology: "tensor" becomes
"array" and "Tensor" becomes "Array". This is for downstream packages such
as \code{anvl}, whose users think in terms of arrays rather than the StableHLO
notion of tensors. stablehlo's own errors are unaffected unless this
function is applied to them.

The substitution is applied on word boundaries, so identifiers such as
\code{TensorType}, \code{hlo_tensor()} or \code{assert_vts_are_tensors()} are left
untouched. A rendered type such as \verb{tensor<2x3xf32>} \emph{is} rewritten to
\verb{array<2x3xf32>}, which is the intended behaviour for array-facing packages.
}
