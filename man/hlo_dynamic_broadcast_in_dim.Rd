% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op-dynamic_broadcast_in_dim.R
\name{infer_types_dynamic_broadcast_in_dim}
\alias{infer_types_dynamic_broadcast_in_dim}
\alias{hlo_dynamic_broadcast_in_dim}
\title{DynamicBroadcastInDim Operator}
\usage{
infer_types_dynamic_broadcast_in_dim(
  operand,
  output_dimensions,
  broadcast_dimensions,
  shape
)

hlo_dynamic_broadcast_in_dim(
  operand,
  output_dimensions,
  broadcast_dimensions,
  shape,
  output_types = NULL
)
}
\arguments{
\item{operand, output_dimensions}{(\code{\link{FuncValue}})\cr}

\item{broadcast_dimensions}{(\code{integer()})\cr
Maps each axis of \code{operand} to an axis of the result.}

\item{shape}{(\code{integer()})\cr
The result's static shape, with \code{NA} at each axis whose size is only known
at run time. It cannot be inferred from the operands, because
\code{output_dimensions} is data.}

\item{output_types}{(\code{list()} of \code{\link{ValueType}} | \code{NULL})\cr Output types known ahead of time (e.g. from type inference at trace time). When provided, type inference and its input validation are skipped.}
}
\value{
\code{\link{FuncValue}}\cr
}
\description{
See \url{https://openxla.org/stablehlo/spec#dynamic_broadcast_in_dim} for details.
}
\details{
Note that \code{shape} is a \emph{claim}, not a check: nothing here can verify it,
since the sizes it describes are data. Where StableHLO can constant-fold the
size operands it will verify the claim itself and reject a wrong one
downstream.
}
