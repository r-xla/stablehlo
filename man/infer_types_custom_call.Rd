% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op-custom_call.R
\name{infer_types_custom_call}
\alias{infer_types_custom_call}
\title{Infer types for custom call}
\usage{
infer_types_custom_call(
  ...,
  call_target_name,
  api_version,
  has_side_effect,
  backend_config = NULL,
  output_types = NULL,
  operand_layouts = NULL,
  result_layouts = NULL,
  output_operand_aliases = NULL
)
}
\arguments{
\item{...}{Input values.}

\item{call_target_name}{(\code{character(1)})\cr
The name of the custom function to call.}

\item{api_version}{(\code{integer(1)})\cr
The API version.}

\item{has_side_effect}{(\code{logical(1)})\cr
Whether the custom call has side effects.}

\item{backend_config}{(\code{list} | \code{NULL})\cr
Optional backend configuration as a named list.}

\item{output_types}{(\code{list} of \code{\link{ValueType}} | \code{NULL})\cr
The output types of the custom call. Default is NULL (no outputs).}

\item{operand_layouts, result_layouts}{Layouts, checked but not used for
type inference.}

\item{output_operand_aliases}{Aliases (not used for type inference; named
only so that \code{hlo_fn()} does not pass it through \code{...} as an operand).}
}
\value{
(\code{ValueTypes})\cr
The output types (empty for side-effect only calls).
}
\description{
Infer the output types for a custom call operation.
}
