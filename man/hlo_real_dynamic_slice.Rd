% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op-real_dynamic_slice.R
\name{infer_types_real_dynamic_slice}
\alias{infer_types_real_dynamic_slice}
\alias{hlo_real_dynamic_slice}
\title{Real Dynamic Slice Operator}
\usage{
infer_types_real_dynamic_slice(
  operand,
  start_indices,
  limit_indices,
  strides,
  shape
)

hlo_real_dynamic_slice(
  operand,
  start_indices,
  limit_indices,
  strides,
  shape,
  output_types = NULL
)
}
\arguments{
\item{operand}{(\code{\link{FuncValue}})\cr
The array to slice.}

\item{start_indices, limit_indices, strides}{(\code{\link{FuncValue}} | \code{\link{ValueType}})\cr
Rank-1 integer tensors, one element per axis of \code{operand}.}

\item{shape}{(\code{integer()})\cr
The result's static shape, with \code{NA} at each axis whose size is only known
at run time.}

\item{output_types}{(\code{list()} of \code{\link{ValueType}} | \code{NULL})\cr
Output types known ahead of time (e.g. from type inference at trace
time). When provided, type inference and its input validation are
skipped.}
}
\value{
\code{\link{FuncValue}}
}
\description{
Slices \code{operand} with start, limit and stride vectors supplied as \emph{values}
rather than attributes, so the result's extents may be computed by the
program -- from its input data, even. That is what distinguishes it from
\code{\link[=hlo_slice]{hlo_slice()}} (all three static) and \code{\link[=hlo_dynamic_slice]{hlo_dynamic_slice()}} (dynamic
start, but static sizes, hence a static result shape).

This op is in the StableHLO dialect but \strong{not} in SPEC.md, which documents
no op with a data-dependent result extent. It is here because it is the only
way to express one: a program that computes how much of a buffer is live --
the count of distinct elements, of rows passing a filter -- and returns just
that much.

Note that XLA cannot compile it (\verb{can't be translated to XLA HLO}), and
shape refinement cannot remove it either, since no shape in the program
determines the extent. It needs a backend that compiles dynamic shapes.
}
