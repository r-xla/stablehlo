% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/op-scan.R
\name{hlo_scan}
\alias{hlo_scan}
\title{Scan (left-/right-fold along a dimension)}
\usage{
hlo_scan(inputs, init, body, dim, reverse = FALSE)
}
\arguments{
\item{inputs}{(\code{list()} of \code{\link{FuncValue}})\cr
One or more tensors, all sharing the same shape. The scan iterates over
\code{dim}; per-iteration each input contributes a slice with \code{dim} removed.}

\item{init}{(\code{\link{FuncValue}})\cr
The initial carry. Its \code{ValueType} is the carry type for every iteration
and the carry returned at the end of the scan.}

\item{body}{(\code{function})\cr
Body closure called once during IR construction with signature
\verb{function(carry, ...elems) -> list(new_carry, out_elem)}:
\itemize{
\item \code{carry} has the same \code{ValueType} as \code{init}.
\item \code{elems...} are slices: one per element of \code{inputs}, each with \code{dim}
removed from its shape and the dtype of the matching input.
\item \code{new_carry} must match \code{init}'s \code{ValueType}.
\item \code{out_elem} must have shape equal to the slice shape (i.e. \code{inputs[[1]]}'s
shape with \code{dim} removed) and dtype matching \code{inputs[[1]]}.
}}

\item{dim}{(\code{integer(1)})\cr
Zero-based axis to scan over.}

\item{reverse}{(\code{logical(1)})\cr
If \code{TRUE}, the scan walks \code{dim} from \code{n - 1} down to \code{0}. Default \code{FALSE}.}
}
\value{
(\code{\link{FuncValue}})\cr
The stacked outputs, with the same shape and dtype as \code{inputs[[1]]}.
}
\description{
High-level helper that constructs IR for a scan along a single dimension.
At each step the user-supplied \code{body} closure receives the current carry
and a slice of each input along \code{dim}, and returns the updated carry and
an output element. Output elements are stacked back along \code{dim}, producing
a result with the same shape as \code{inputs[[1]]}.

Internally \code{hlo_scan} decomposes into a \link[=hlo_while]{stablehlo.while}
loop with \code{dynamic_slice} reads and \code{dynamic_update_slice} writes — there
is no dedicated \code{scan} op in StableHLO. The resulting IR runs on any
backend that implements \code{while}, but execution is sequential along \code{dim}.
}
